#include <iostream>
#include <stdexcept>

void downloadStreamingAPK(const std::string& service) {
    try {
        if (service.empty()) {
            throw std::invalid_argument("Service name cannot be empty.");
        }
        std::cout << "Downloading APK for " << service << "..." << std::endl;
        // Simulate downloading process
        // Add actual downloading logic here
    } catch (const std::exception& e) {
        std::cerr << "Error downloading APK: " << e.what() << std::endl;
    }
}
